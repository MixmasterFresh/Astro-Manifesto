#require brings in some package from the astro path. These would be packages downloaded by a package manager.

require "some_package"

#include brings in code from a path relative to the build root

include "some/random/path.as"

# include_relative brings in code from a path relative to current directory.
